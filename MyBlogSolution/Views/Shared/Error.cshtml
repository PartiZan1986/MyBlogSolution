@{
    ViewData["Title"] = "Ошибка";
    Layout = "_Layout";
}

<div class="row justify-content-center">
    <div class="col-md-8 text-center">
        <div class="card">
            <div class="card-body py-5">
                <!-- Иконка -->
                <div class="mb-4">
                    <i class="fas fa-exclamation-triangle fa-5x text-danger"></i>
                </div>

                <!-- Заголовок -->
                <h1 class="display-4 text-danger mb-3">Что-то пошло не так</h1>

                <!-- Сообщение -->
                <p class="lead mb-4">
                    Произошла непредвиденная ошибка. Мы уже работаем над её устранением.
                </p>

                <!-- Дополнительная информация -->
                <div class="alert alert-warning mb-4">
                    <h5><i class="fas fa-tools"></i> Техническая информация:</h5>
                    <p class="mb-0">
                        @if (ViewBag.ErrorMessage != null)
                        {
                            <strong>Сообщение:</strong> 
                            @ViewBag.ErrorMessage
                        
                            <br>
                        }
                        <strong>Время:</strong> @DateTime.Now.ToString("dd.MM.yyyy HH:mm:ss")<br>
                        <strong>Запрос:</strong> @Context.Request.Method @Context.Request.Path
                    </p>
                </div>

                <!-- Кнопки действий -->
                <div class="d-flex justify-content-center gap-3 flex-wrap">
                    <a href="javascript:history.back()" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Вернуться назад
                    </a>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                        <i class="fas fa-home"></i> На главную
                    </a>
                    <button onclick="location.reload()" class="btn btn-success">
                        <i class="fas fa-redo"></i> Обновить страницу
                    </button>
                </div>

                <!-- Контакты поддержки -->
                <div class="mt-4 pt-3 border-top">
                    <h6><i class="fas fa-headset"></i> Нужна помощь?</h6>
                    <p class="text-muted mb-0">
                        Свяжитесь с технической поддержкой:
                        <a href="mailto:support@myblog.com" class="text-primary">support@myblog.com</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>